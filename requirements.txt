# App dependencies — lightweight, change more often than requirements-ml.txt.
# Avoid pinning (==) packages that are also transitive deps of requirements-ml.txt
# (pydantic, aiohttp, uvicorn, starlette) — those pins downgrade versions that
# chromadb/langchain already resolved to newer compatible releases.

# === API CORE ===
fastapi>=0.104.1
uvicorn>=0.24.0
python-dotenv>=1.0.0

# === HTTP CLIENT ===
aiohttp>=3.9.1

# === DATA PROCESSING ===
pypdf==3.17.0
python-docx==1.1.0
python-multipart==0.0.6
openpyxl==3.1.2

# === UTILITIES ===
python-jose==3.3.0
passlib==1.7.4
python-dateutil>=2.8.2
